#1064
cont = 0
soma = 0

for i in range(6):
  valor = float(input())
  if valor > 0:
    cont += 1
    soma += valor
    media = soma / cont

print(f"{cont} valores positivos")
print(f"{media:.1f}")
    
